import 'package:flutter/material.dart';

class Beginner_Class extends StatelessWidget {
  const Beginner_Class({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
