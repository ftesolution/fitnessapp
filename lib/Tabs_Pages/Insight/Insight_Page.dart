import 'package:flutter/material.dart';

class Insight_Page extends StatelessWidget {
  const Insight_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
